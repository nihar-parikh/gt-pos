import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ethicsinfotech.gtpos',
  appName: 'GT-POS',
  webDir: '../web/dist',
};

export default config;
