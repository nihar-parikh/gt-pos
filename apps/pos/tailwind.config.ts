import { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: '#004aad',
        accent: '#f5c518',
      },
      fontFamily: {
        sans: ['DmartFont', 'sans-serif'],
      },
    },
  },
};

export default config;
